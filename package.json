{
  "name": "sprite",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "cd packages/app && yarn start",
    "deploy": "now && now alias"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
